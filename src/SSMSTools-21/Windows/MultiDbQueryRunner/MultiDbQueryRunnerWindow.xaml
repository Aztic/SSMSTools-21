<Window x:Class="SSMSTools_21.Windows.MultiDbQueryRunner.MultiDbQueryRunnerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:SSMSTools_21.Windows.MultiDbQueryRunner"
        Height="560" Width="460"
        ResizeMode="NoResize"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Selected server -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock FontWeight="SemiBold" Margin="0,0,0,2"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}">Selected server:</TextBlock>
            <TextBlock Text="{Binding ServerName}"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        </StackPanel>

        <!-- Saved groups -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock FontWeight="SemiBold" Margin="0,0,0,4"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}">Saved groups:</TextBlock>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" Margin="0,0,8,0"
                          Name="DatabaseGroupsCombobox"
                          ItemsSource="{Binding Path=DatabaseGroups}"
                          DisplayMemberPath="Title"
                          SelectedValue="{Binding Path=SelectedDatabaseGroup, Mode=TwoWay}"
                          SelectionChanged="DatabaseGroupsCombobox_SelectionChanged" />
                <ui:Button Grid.Column="1"
                           Name="EditButton"
                           Click="EditButton_Click"
                           Appearance="Secondary"
                           IsEnabled="False"
                           Visibility="Hidden">
                    Create
                </ui:Button>
            </Grid>
        </StackPanel>

        <!-- Show system databases Checkbox -->
        <CheckBox Grid.Row="2"
                  Content="Show system databases"
                  IsChecked="{Binding IsShowSystemDatabasesSelected, Mode=TwoWay}"
                  Margin="0,0,0,6"/>

        <!-- Select All Checkbox -->
        <CheckBox Grid.Row="3"
                  Content="Select All"
                  IsChecked="{Binding IsAllSelected, Mode=TwoWay}"
                  Margin="0,0,0,8"/>

        <!-- Scrollable List of databases -->
        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
            <ItemsControl ItemsSource="{Binding Databases}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Margin="0,2,0,0">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Script content label -->
        <TextBlock Grid.Row="5" FontWeight="SemiBold" Margin="0,0,0,4"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}">Script content:</TextBlock>

        <!-- Multiline TextBox -->
        <ui:TextBox Grid.Row="6"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto"
                    Height="100"
                    Margin="0,0,0,12"
                    Text="{Binding QueryContent, Mode=TwoWay}" />

        <!-- Action buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="CancelButton_Click" Appearance="Secondary" />
            <ui:Button Content="Execute" Width="90" Click="ExecuteButton_Click" Appearance="Secondary" />
        </StackPanel>
    </Grid>

</Window>
